<?xml version="1.0" encoding="utf-8"?>

<com.vendomatica.vendroid.ScrimInsetsFrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/scrimInsetsFrameLayout"
    android:layout_width="300dp"
    android:layout_height="match_parent"
    android:layout_gravity="start"
    android:background="@android:color/white"
    android:elevation="10dp"
    android:fitsSystemWindows="true"
    app:insetForeground="#4000"
    tools:showIn="@layout/activity_main">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="left|start"
        android:background="@android:color/white"
        android:orientation="vertical">

        <ImageView
            android:layout_width="match_parent"
            android:layout_height="150dp"
            android:src="@drawable/navheader"
            android:scaleType="centerCrop" />

        <TextView
            android:text="TextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginLeft="@dimen/space_40"
            android:textStyle="bold"
            android:textSize="@dimen/space_20"
            android:textColor="@color/color_vendomatica"
            android:id="@+id/txtNombre" />

        <ListView
            android:id="@+id/navdrawerlist"
            android:layout_height="match_parent"
            android:layout_width="match_parent"
            android:divider="@null"
            android:choiceMode="singleChoice" />

    </LinearLayout>

</com.vendomatica.vendroid.ScrimInsetsFrameLayout>